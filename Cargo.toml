[package]
name = "rust_os"
version = "0.1.0"
authors = ["j3elam <j3elam@gmail.com>"]
edition = "2018"

[dependencies]

# Disable stack unwinding on panic, instant abort
[profile.dev] 
panic = "abort"
[profile.release]
panic = "abort"